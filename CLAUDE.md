# CLAUDE.md

このファイルは、Claude Code がこのリポジトリで作業する際のガイドです。

## プロジェクト概要

このプロジェクトは **Higgs Field**(MCP ツール群: `mcp__higgs_field__*`)を使って
**ファッション系の動画**(試着・OOTD・ルックブック・プロダクト紹介・UGC風広告など)を
制作することを目的としています。主な配信先は Instagram Reels / TikTok / YouTube Shorts
などの縦型ショート動画です。

## 作業を始める前に必ず行うこと

UGC動画・広告・プロダクト動画など「ブリーフに沿った複数ステップの動画制作」を頼まれたら、
直接 `generate_video` を叩く前に **必ず** `mcp__higgs_field__get_workflow_instructions`
を呼び、該当するワークフロー(SKILL)をロードすること。どのワークフローが存在するかは
毎回カタログで確認し、憶測で決めない。

## このプロジェクトでよく使うワークフロー

ファッション動画制作で頻出するのは主に以下。実際に使う際は
`get_workflow_instructions({ workflow: "<name>" })` で詳細を読み込む。

| ワークフロー名 | 用途 |
|---|---|
| `ugc-try-on-video` | 特定の商品(服・アクセサリー等)を着用してポーズを取る「試着 / OOTD / フィットチェック」動画 |
| `ugc-review-video` | 出演者(実在 or 生成)がカメラに向かって商品を紹介するレビュー風動画 |
| `ugc-unboxing-video` | 開封・初見リアクション動画 |
| `ugc-tutorial-video` | 着こなし方などのステップ解説動画(画面に "Step N" 表示) |
| `product-photoshoot` | スタジオ撮影風・ライフスタイル風の商品スチール、ルックブック用静止画 |
| `character-sheet` | 一貫したモデル/キャラクターの参照シート作成(複数アングル・表情) |
| `ad-multiplier` | 既存の4〜30秒動画から、人物や商品を差し替えた複数バリエーションを量産 |
| `thumbnail-generation` | サムネイル・カバー画像の制作 |
| `subtitles` | 完成動画へのハードサブ(焼き込み字幕)追加 |

上記に当てはまらない単発のイメージ/動画生成は `generate_image` / `generate_video` を直接使う。

## 素材・キャラクターの一貫性

- 同じモデル(人物)を複数カットで使い回したい場合は `show_characters`(Soul)で
  トレーニングするか、`show_reference_elements` で参照エレメントとして保存する。
  - Soul: 同一人物のみ、5〜20枚の写真が必要、`soul_2` / `soul_cinematic` 専用。
  - Elements: 即時利用可能、複数人物/複数被写体もOK、他の主要モデルでも使える。
- ローカルの画像・動画をアップロードする場合はファイルパスを直接扱わず、
  必ず `media_upload_widget` を最初に呼び出してアップロードを行う。

## 出力形式のデフォルト

- SNSショート動画向けは基本 `aspect_ratio: "9:16"`(縦型)を明示する。
  横型が必要な場合のみ `16:9` を指定する。
- 生成前にコストを確認したい場合は `get_cost: true` を付けてプレフライトする。

## リポジトリ構成

現時点ではリポジトリは空の状態からスタートしている。今後、以下のような構成を想定:

- `prompts/` — 動画・画像生成に使うプロンプトや台本
- `assets/source/` — アップロード元の商品写真・参照動画などの素材
- `assets/output/` — 生成された動画・画像の書き出し先
- `docs/` — 企画書、ブランドガイドラインなどのドキュメント

新しくディレクトリ構成を作る際は、実際の作業内容に合わせて上記を調整して問題ない。

## 開発上の注意

- Higgs Field 側の生成物(動画・画像ファイルそのもの)は基本的に git 管理対象外とし、
  `.gitignore` で除外する。リポジトリにはプロンプト・スクリプト・設定などの
  再現可能なテキスト資産のみをコミットする。
- 商品や出演者の権利・肖像権に関わる生成(実在の人物の写真からのUGC生成等)は、
  本人の同意が取れているものだけを対象とする。
