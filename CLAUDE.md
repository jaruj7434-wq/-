# CLAUDE.md

このファイルは、Claude Code がこのリポジトリで作業する際のガイドです。

## リポジトリ概要

このリポジトリでは **Higgs Field**(MCP ツール群: `mcp__higgs_field__*`)を使って
複数ジャンルの動画・画像制作を行います。ジャンルごとにフォルダを分けており、
それぞれに専用の `CLAUDE.md` があります。作業対象のジャンルに応じて、該当フォルダの
`CLAUDE.md` を必ず読み込んでから作業すること。

| フォルダ | 用途 |
|---|---|
| [`fashion/`](fashion/CLAUDE.md) | ファッション系動画(試着・OOTD・ルックブック・UGC風広告など) |

新しいジャンルの動画制作を頼まれた場合は、同様に新しいフォルダと `CLAUDE.md` を
作成して対応する。ジャンルが曖昧な依頼の場合はユーザーにどのフォルダ(または
新規フォルダ)で作業するか確認する。

## Higgs Field 生成物の運用ルール(ユーザー指定・全ジャンル共通)

- **生成前に一言確認する**: `generate_image` / `generate_video` / `generate_audio` /
  `*_batch` など、クレジットや処理時間が発生する生成系ツールを呼び出す前には、
  必ずチャットで一言ユーザーに確認を取ってから実行すること。分析系
  (`video_analysis_*`)やアップロード(`media_upload_widget`)、要素登録
  (`show_reference_elements`)など課金を伴わない準備作業はこの限りではないが、
  疑わしい場合は確認を取る。
- **クレジットを消費しない設定を優先する**: 生成を実行する際は、可能な限り
  `use_unlim: true`(無制限/トライアル枠)を使い、通常クレジットを消費しないように
  する。ただし、無制限枠がプラン上使えない場合はツール側からその旨が返ってくるため、
  その場合は必ずユーザーに「クレジットを消費する形になるがよいか」を確認してから
  進める。無断でクレジットを消費する生成を行わない。

## 開発上の注意(全ジャンル共通)

- Higgs Field 側の生成物(動画・画像ファイルそのもの)は基本的に git 管理対象外とし、
  `.gitignore` で除外する。リポジトリにはプロンプト・スクリプト・設定などの
  再現可能なテキスト資産のみをコミットする。
- ジャンル固有の権利まわりの注意(肖像権・版権など)は各フォルダの `CLAUDE.md` を参照。
